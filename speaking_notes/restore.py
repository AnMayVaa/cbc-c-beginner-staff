import os
import re

dir_path = r"C:\Antigravity\cbc-c-beginner-staff\speaking_notes"

# We must read from the ORIGINAL contents or run git checkout first?
# Yes, let's git checkout to restore the original files before running the script.
